interface:
  display_name: "LocalWeb"
  short_description: "Local Web visual and context companion for CLI agents."
  default_prompt: "Use LocalWeb to start a local project-level visual interface, render agent output as interactive HTML panels, and collect optional low-risk Markdown context for the CLI."
